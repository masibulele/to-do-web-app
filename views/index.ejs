<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-do List</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <script src="https://kit.fontawesome.com/84102f696b.js" crossorigin="anonymous"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <div class="row  justify-content-center" >
            <div class="to-do bg-light col-lg-6 rounded m-5 shadow">
                <!-- header section of app -->
                <div class="header col">

                    <!--app title -->
                    <div class="to-do-title text-primary p-1 text-start  h1">
                        <i class="fa fa-check bg-primary text-white rounded p-2"></i>
                        <h1>My To-Do List</h1>
                    </div>
                    
                    <!-- input list item -->
                    <div class="to-do-input ">
                        <form action="/submit" method="post" class="input-group">
                            <input type="text" class="form-control" name="entry" placeholder="Add new task..">
                            <button class="btn btn-primary">Add</button>
                        </form> 
                    
                    </div>
                    <hr>

            
                </div>
                <!-- filter section of App -->
                <div class="row justify-content-end">
                    <div class="filter-section col-3 ">
                        <select class="form-select">
                            <option value="Filter" selected>Filter</option>
                            <option value="Today">Today</option>
                            <option value="Work">Work</option>

                        </select>

                    
                    </div>
                </div>
                <!-- list section of app -->
                <div class="to-do-list form-check">
                    
                     <%if(locals.items){%>
                    <% for (let i = 0; i< items.length; i++) { %>
                    <div class="line">
                        <input class="form-check-input" type="checkbox" name="" id="flexCheckDefault">
                        <label class="form-check-label" for="flexCheckDefault"> <%=items[i]%></label>
                    </div>
                    <% } %>
                    <% } else{ %>
                        <div class="line">
                            <input class="form-check-input" type="checkbox" name="" id="flexCheckDefault">
                            <label class="form-check-label" for="flexCheckDefault"> None</label>
                        </div>

                    <% } %>
                </div>
            </div>
        </div>
    </div>
    
</body>
</html>